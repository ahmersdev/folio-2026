// Approximates the reference site's line-fade band using the same
// shrinking-height/fixed-gap technique as the preloader's strip, just with
// more steps for a subtler fade from the page's white bg into solid black.
export const FOOTER_LINE_HEIGHTS_PX = [3, 4, 6, 9];
export const FOOTER_LINE_GAP_PX = 4;
